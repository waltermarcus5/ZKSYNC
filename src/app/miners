import { Users, Cpu, Clock } from "lucide-react";
import { SiteFooter } from "@/components/footer";
import { Logo } from "@/components/logo";
import { Button } from "@/components/ui/button";
import Link from "next/link";

export default function MinersPage() {
  return (
    <div className="flex flex-col min-h-dvh bg-background text-foreground">
      <header className="sticky top-0 z-50 w-full border-b border-border/40 bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60">
        <div className="container mx-auto flex h-16 items-center justify-between">
          <div className="flex items-center gap-6">
            <Logo />
          </div>
           <Button asChild className="font-bold bg-primary text-primary-foreground hover:bg-primary/90">
              <Link href="/">Return to Airdrop</Link>
            </Button>
        </div>
      </header>

      <main className="flex-1 flex items-center justify-center">
        <div className="container mx-auto px-4 py-20 text-center">
          <div className="max-w-2xl mx-auto bg-card p-10 rounded-xl shadow-2xl border border-primary/20">
            <div className="flex justify-center mb-6">
                <div className="bg-primary/10 text-primary rounded-full p-5 border-2 border-primary/20">
                    <Cpu className="w-16 h-16" />
                </div>
            </div>
            <h1 className="font-headline text-4xl sm:text-5xl font-bold mb-4">
              Miner Program: The Waitlist is Open
            </h1>
            <p className="text-muted-foreground text-xl mb-8">
              The ZKsync mining protocol is currently in a pre-launch phase. We are gathering a foundational community of miners to ensure a robust and decentralized network from day one. The mining portal will become active once our community reaches its initial target size.
            </p>
             <div className="bg-secondary/50 border-t border-b border-secondary p-6 rounded-lg mb-8">
                <div className="flex items-center justify-center text-lg">
                    <Clock className="w-6 h-6 mr-3 text-primary" />
                    <span className="font-semibold">Status:</span>
                    <span className="ml-2 text-muted-foreground">Building miner cohort. Check back for updates.</span>
                </div>
            </div>
            <Button asChild size="lg" className="font-bold text-lg bg-primary text-primary-foreground hover:bg-primary/90 shadow-lg shadow-primary/20">
              <Link href="/">Explore the Airdrop</Link>
            </Button>
          </div>
        </div>
      </main>

      <SiteFooter />
    </div>
  );
}
